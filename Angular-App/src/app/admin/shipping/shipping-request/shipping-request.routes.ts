import { Routes } from '@angular/router';
import { ShippingRequestComponent } from './shipping-request.component';

export const shippingRequestRoutes: Routes = [{ path: '', component: ShippingRequestComponent }];
