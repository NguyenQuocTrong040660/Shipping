import { Routes } from '@angular/router';
import { ShippingRequestComponent } from './shiping-request.component';

export const shippingRequestRoutes: Routes = [
  { path: '', component: ShippingRequestComponent }
];
