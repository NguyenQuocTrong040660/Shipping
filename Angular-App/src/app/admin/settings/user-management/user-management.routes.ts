import { Routes } from '@angular/router';
import { UserManagementComponent } from './user-management.component';

export const userRoutes: Routes = [{ path: '', component: UserManagementComponent }];
